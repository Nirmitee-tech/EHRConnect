/**
 * Forms Layout
 * Full-screen layout for forms pages without top navigation
 */

export default function FormsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
